1. Does it make any difference with Linked List?
Answer: It is a lot more faster and efficient with Linked List. The code takes less time to execute

2. What happens if you use list.remove(Integer.valueOf(77))?
Answer: It removes the exact value of 77

3. Express which values are left in the list?
33, 44, 55, and 66

4. list.remove(5); // what does this method do?

It removes the index of 5

5. ist.remove(Integer.valueOf(5)); // what does this one do?

It removes the exact value 5.

